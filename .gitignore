# Binaries & build output
/bin/
# Anchored to the repo root so it only ignores the compiled manager binary,
# NOT the deploy/manager/ manifests directory.
/manager
*.exe
*.test
*.out
cover.out
dist/

# Go workspace files
go.work
go.work.sum

# Local config overrides (keep hack/config.local.yaml tracked)
*.local.env

# OS / editor cruft
.DS_Store
.idea/
.vscode/
*.swp
*~

